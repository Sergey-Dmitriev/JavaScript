const a = () => console.log("a");
const b = () => console.log("b");
const c = a || b ? a() : b();
console.log(c);

Вывод 
a
Почему:
При выполнении выражения `a || b` происходит проверка, является ли `a` истинным значением.
В данном случае, `a` - это функция, которая всегда возвращает `undefined`.
Птому, результатом выражения `a || b` будет `a`, и выполняется вызов функции `a()`, что приводит к выводу "a" в консоль.
